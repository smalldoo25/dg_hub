// js/main.js

import { initializeMenu } from './components/menuManager.js';

document.addEventListener('DOMContentLoaded', () => {
    // Initialize all menu and navigation logic
    initializeMenu();
});